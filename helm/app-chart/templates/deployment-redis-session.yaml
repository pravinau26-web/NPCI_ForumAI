apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-redis-session
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-redis-session
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-redis-session
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-session-service
spec:
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: {{ .Release.Name }}-redis-session
